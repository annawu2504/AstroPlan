# AstroPlan dependencies
# Python 3.10+

# Core async
# asyncio; python_version < "3.10"

# Config
pyyaml>=6.0

# LLM (optional — only needed for real planning, not mock demo)
anthropic>=0.30.0

# Vector search (optional — for future dense-embedding milestone engine)
# faiss-cpu>=1.7.4
# sentence-transformers>=2.6.0

# Web monitor SSE (optional)
# aiohttp>=3.9.0
